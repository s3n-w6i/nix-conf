on:
  pull_request:
  push:
    branches: [master]

jobs:
  build:
    name: Check Nix targets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check nix flake
        run: nix flake check
